# Copy to .env.local and fill in your Supabase project values.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-secret-service-role-key

# Crypto payments are verified directly on-chain via public RPC/explorer reads
# (no third-party processor, no API key required). The defaults below are
# public endpoints; override them with your own (e.g. paid/dedicated RPC) for
# higher rate limits and reliability in production. All optional.
# ETH_RPC_URL=https://eth.llamarpc.com
# BSC_RPC_URL=https://bsc-dataseed.binance.org
# TRON_API_URL=https://api.trongrid.io
# SOL_RPC_URL=https://api.mainnet-beta.solana.com
